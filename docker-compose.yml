services:
  api:
    build: .
    environment:
      PORT: 8080
      APP_ENV: development
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data

  websocket:
    build: .
    command: ["hps-websocket"]
    environment:
      PORT: 8081
    ports:
      - "8081:8081"
